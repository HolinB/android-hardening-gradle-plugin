org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true

# Keep hardening opt-in on the command line:
# -PandroidHardening=true
# For controlled reproducibility checks only:
# -PandroidHardeningFixedSeed=replace-with-a-non-secret-lab-seed
